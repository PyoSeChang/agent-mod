# v0.3.2+ — 2026-03-13

버그 수정 빌드: I-017 (Action 팩토리), I-018 (Memory 스코핑), I-019 (Memory 핫 리로드)

## 실행 환경

- 버전: v0.3.2+ (I-017/I-018/I-019 수정 포함, uncommitted)
- 월드: 기존 농장 월드
- 에이전트: alex, zim (multi-agent)

---

## Run 1 — S-X001: 농사 전체 (multi-agent, 이전 세션 이어서)

**로그**: `2026-03-13_200426.jsonl` (세션 계속)

### Benchmark

| 지표 | 값 |
|------|-----|
| 총 액션 | 29 |
| 성공/실패 | 25 / 4 |
| 소요시간 | ~405초 (6분 45초) |
| substep 실패 | 214건 |

### 액션 분석

| 액션 | 호출 | 성공 | 실패 | 평균(s) |
|------|------|------|------|---------|
| execute_sequence | 17 | 15 | 2 | 23.3 |
| move_to | 6 | 5 | 1 | 1.5 |
| open_container | 2 | 2 | 0 | 0.0 |
| close_container | 2 | 2 | 0 | 0.0 |
| place_block | 1 | 0 | 1 | 0.0 |
| mine_area | 1 | 1 | 0 | 0.2 |

### 실패 분석

| 실패 | 원인 | 이슈 |
|------|------|------|
| execute_sequence x2 | move_to "No path found" (물 웅덩이 이후 경로 차단), use_item_on "Block too far away" (dist 5.7 > max 4.5) | 기존 I-015 관련 |
| place_block x1 | "No matching item in inventory for water" — water_bucket을 place_block으로 시도 | brain 이슈 (프롬프트) |
| move_to x1 | 경로 없음 | 지형 변경 후 재시도로 해결 |

### substep 실패 패턴 (214건)

| 패턴 | 건수 | 원인 |
|------|------|------|
| `interaction_not_consumed: PASS` (above=grass) | ~27 | 잔디 위 블록 → 호미 상호작용 불가 (I-005, 기존) |
| `interaction_not_consumed: PASS` (item=empty) | ~81 | 씨앗 소진 후 빈 손으로 계속 순회 (I-014) |
| `interaction_not_consumed: PASS` (farmland, diamond_hoe) | ~80 | 이미 경작된 farmland에 호미 재사용 → PASS (중복 경작 시도) |
| `interaction_not_consumed: PASS` (farmland, empty) | ~16 | 빈 손으로 farmland에 상호작용 |
| `interaction_not_consumed: FAIL` (water) | 1 | 물 블록에 씨앗 심기 시도 |
| `Block too far away` | 1 | dist 5.7 > max 4.5 |

### I-017 검증 (Action 팩토리)

| 항목 | 결과 |
|------|------|
| 빌드 | 성공 |
| 서버 시작 | 정상 (ActionRegistry 초기화 로그 확인 필요) |
| 단일 에이전트 move_to | 정상 (211200 로그: 1회 성공) |
| multi-agent 동시 실행 | **미검증** — 이번 세션에서 동시 액션 로그 없음 |

### I-018 검증 (Memory 스코핑)

| 항목 | 결과 |
|------|------|
| per-agent memory 생성 | **성공** — m003 `vt=[zim]`, m004 `vt=[alex]` |
| 스코핑 자동 적용 | **성공** — MCP remember() → `/agent/{name}/memory/create` → visibleTo 자동 설정 |
| 기존 global memory 유지 | **성공** — m001, m002 그대로 |

### I-019 검증 (Memory 핫 리로드)

| 항목 | 결과 |
|------|------|
| `/memory/reload` 엔드포인트 | 빌드 포함, **실행 검증 미완** |

---

## 발견 이슈

- [ ] `I-020`: 이미 경작된 farmland에 호미 재사용 시도 — use_item_on_area가 블록 상태(farmland vs grass_block) 필터링 없음. brain이 한 번 경작한 영역 전체를 다시 호미로 돌림 (80건 낭비)
- [ ] `I-021`: use_item_on_area에서 item=empty (씨앗 소진) 시 조기 중단 없음 — 81블록 전체 순회 (I-014 재확인, 여전히 미해결)
- [ ] `I-022`: `place_block water` 시도 — brain이 water_bucket → use_item_on 대신 place_block water 호출. 프롬프트에 "water/lava는 bucket + use_item_on으로" 가이드 필요
- [ ] `I-023`: use_item_on에서 distance 5.7 허용 후 "Block too far away" — execute_sequence 내 move_to 없이 먼 블록에 use_item_on 직접 호출

## 종합 평가

- I-017: 빌드+단일 에이전트 정상 동작 확인. **동시 실행 테스트 필요**.
- I-018: per-agent memory 스코핑 동작 확인 (m003=zim, m004=alex).
- I-019: 코드 포함, 실행 테스트 필요.
- 기존 이슈(I-014 씨앗 소진, I-015 y좌표)는 여전히 미해결.
- 새 이슈 4건 발견 (I-020~I-023).
