# v0.2.3 — 2026-03-12

## 실행 환경

- 버전: v0.2.3 (commit `c295c92` + unstaged)
- 월드: New World
- 에이전트 스폰: 상자 근처

---

## Run 1 — 중간 빌드 (click_slot 미제거): 상자에서 아이템 꺼내기

**명령**: 상자에서 필요한 물건 꺼내기

### Benchmark

| 지표 | 값 |
|------|-----|
| 턴 | 16 |
| 소요시간 | ~1분 |
| 컨테이너 open 횟수 | 3 |
| click_slot 횟수 | 6 |
| container_transfer 사용 | 0 |

### 로그 분석 (Claude)

**긍정적:**
- `open_container`가 `slots` 배열 정상 반환 — brain이 정확한 슬롯 번호(21, 22, 23)를 즉시 식별
- v0.2.2 대비 get_observation 불필요 — 슬롯 정보만으로 판단

**문제점:**
- `container_transfer` 미사용 — brain이 익숙한 `click_slot`을 선호
- open/close 3회 반복 — quick_move 후 아이템 확인 불가로 재시도

---

## Run 2 — 중간 빌드 (click_slot 미제거): 상자 + 화로 조작

**명령**: 상자에서 아이템 꺼내서 화로에 넣기

### Benchmark

| 지표 | 값 |
|------|-----|
| 턴 | 47 |
| 소요시간 | ~3분 |
| 컨테이너 open 횟수 | 16 |
| click_slot 횟수 | 12 (직접) + 24 (execute_sequence 경유) |
| container_transfer 사용 | 0 |

### 로그 분석 (Claude)

**문제점:**
- `I-011`: brain이 `execute_sequence` 안에 `click_slot` action을 직접 호출 — MCP 도구 설명 우회
- `I-012`: 화로 제련 대기를 위해 open/close 반복 폴링 (10회 이상) — 제련 완료 감지 메커니즘 없음
- `container_transfer` 완전 무시 — 프롬프트 가이드만으로는 행동 변화 불충분

### 대응

- maxLines 10→20 증가
- **전략 전환**: container_transfer MCP 제거 → click_slot 복원 + 피드백 강화 (before/after/carried 반환)
  - 근거: brain이 container_transfer 완전 무시, 환각으로 click_slot 호출. 모델 prior와 싸우지 말고 활용.
  - ClickSlotAction 파라미터명 다형성 추가 (`action`/`click_action`/`action_type`)

---

## Run 3 — I-013 수정 후: 상자에서 아이템 전체 꺼내기

**명령**: 상자에서 아이템 꺼내기

### Benchmark

| 지표 | 값 |
|------|-----|
| 턴 | 6 |
| 소요시간 | ~45초 |
| 컨테이너 open 횟수 | 1 |
| click_slot 횟수 | 27 (execute_sequence 경유) |
| open/close 반복 | 0 |

### 로그 분석 (Claude)

**긍정적:**
- `open_container` → 27개 spruce_log 즉시 파악
- `execute_sequence`로 27개 click_slot(`action_type: "quick_move"`) 일괄 실행 — **26ms에 전부 완료**
- `action: "quick_move"` 정상 파싱 확인 (I-013 수정 효과)
- open 1회 → sequence 1회 → close 1회. **최적 패턴 달성.**

**Run 2 대비 개선:**
- 턴: 47 → 6 (**87% 감소**)
- open/close: 16 → 1
- 불필요한 재시도: 0

### 유저 관찰

대성공. 6턴만에 완료.

---

## 발견 이슈

- [x] `I-008`: open_container 슬롯 반환 + click_slot 피드백 강화
- [x] `I-009`: relayToChat 줄바꿈 처리 (maxLines 20줄로 증가)
- [x] `I-010`: click_slot 유지로 환각 호출 방지 — 모델 prior 활용 전략
- [x] `I-013`: click_slot `action` 필드 우선순위 충돌 수정 — quick_move 정상 동작 확인
- [ ] `I-011`: execute_sequence가 MCP 미등록 action도 호출 가능 — ActionRegistry 직접 접근
- [ ] `I-012`: 화로 제련 대기 — open/close 폴링. 제련 완료 이벤트 또는 대기 메커니즘 필요

---

## 다음 버전 우선순위

1. `I-012` 화로 제련 대기 — 비동기 제련 완료 감지 또는 smelt action 개선
2. 컨테이너 넣기 테스트 (꺼내기는 검증됨, 넣기+화로 조작 미검증)
